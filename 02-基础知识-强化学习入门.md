---
title: "02 - 基础知识：强化学习入门"
description: "掌握强化学习的核心概念，为理解 Agentic RL 打下基础"
difficulty: "入门"
reading_time: "20 min"
tags: ["RL", "强化学习", "MDP", "POMDP", "基础"]
---

# 02 - 基础知识：强化学习入门

> **学习目标**：掌握强化学习（Reinforcement Learning, RL）的核心概念，为理解 Agentic RL 打下基础。

---

## 目录

- [2.1 什么是强化学习？](#21-什么是强化学习)
- [2.2 核心概念详解](#22-核心概念详解)
- [2.3 MDP（马尔可夫决策过程）](#23-mdp马尔可夫决策过程)
- [2.4 从 MDP 到 POMDP](#24-从-mdp-到-pomdp)
- [2.5 Exploration vs Exploitation（探索与利用）](#25-exploration-vs-exploitation探索与利用)
- [2.6 On-Policy vs Off-Policy](#26-on-policy-vs-off-policy)
- [2.7 RL 的两大方法](#27-rl-的两大方法)
- [2.8 术语速查](#28-术语速查)
- [2.9 小结](#29-小结)
- [练习题](#练习题)

---

## 2.1 什么是强化学习？

强化学习是一种机器学习范式，Agent（智能体）通过与 Environment（环境）交互，根据 Reward（奖励）信号学习最优的 Policy（策略）。

**类比理解**：训练小狗——小狗做对了给零食（正奖励），做错了不给（负奖励/无奖励），小狗慢慢学会什么行为能获得更多零食。

---

## 2.2 核心概念详解

### 2.2.1 Agent（智能体）

做决策的主体。在 Agentic RL 中，Agent 就是 LLM 模型。

### 2.2.2 Environment（环境）

Agent 交互的外部世界。在 Agentic RL 中可以是：
- 网页浏览器（Web Browsing）
- 代码沙盒（Code Sandbox）
- 游戏世界（Game World）
- 终端命令行（Terminal / Shell）

### 2.2.3 State（状态）与 Observation（观测）

- **State（状态）**：环境的完整描述
- **Observation（观测）**：Agent 能看到的（可能是状态的一部分）

> 在 Agent 场景中，Agent 通常无法看到环境的完整状态——这涉及到 **POMDP** 的概念，将在本章 [2.4 节](#24-从-mdp-到-pomdp) 初步介绍，并在第 04 章详细展开。

### 2.2.4 Action（动作）

Agent 在某个状态下做出的选择。对 LLM Agent 来说：
- 生成一段文本回复
- 调用一个工具（如搜索、代码执行）
- 决定终止当前任务

### 2.2.5 Reward（奖励）

环境给 Agent 的数值反馈：

| 类型 | 说明 | 示例 |
|------|------|------|
| **Sparse Reward（稀疏奖励）** | 只在任务结束时给奖励 | 任务成功 +1，失败 0 |
| **Dense Reward（稠密奖励）** | 每步都有奖励 | 每正确一步 +0.1 |
| **Outcome Reward（结果奖励）** | 基于最终结果 | 答案正确/错误 |
| **Process Reward（过程奖励）** | 基于推理/行动过程 | 使用了正确的工具 +0.05 |

### 2.2.6 Policy（策略）

Agent 根据观测选择动作的规则。LLM 本身就是一个 Policy——输入观测（对话历史），输出动作（回复/工具调用）。

- **π(a|s)**：在状态 s 下选择动作 a 的概率

### 2.2.7 Value Function（价值函数）

评估"处于某个状态有多好"：
- **V(s)**：状态 s 的期望累积奖励
- **Q(s, a)**：在状态 s 下执行动作 a 后的期望累积奖励

---

## 2.3 MDP（Markov Decision Process，马尔可夫决策过程）

RL 问题的数学形式化：

```
MDP = (S, A, P, R, γ)

S  = State Space（状态空间）：所有可能状态的集合
A  = Action Space（动作空间）：所有可能动作的集合
P  = Transition Probability（转移概率）：P(s'|s,a) 从状态 s 执行 a 后转移到 s' 的概率
R  = Reward Function（奖励函数）：R(s,a) 在状态 s 执行动作 a 获得的奖励
γ  = Discount Factor（折扣因子）：0 ≤ γ ≤ 1，未来奖励的衰减系数
```

**关键公式 —— 累积回报（Return）**：

```
G_t = R_{t+1} + γ·R_{t+2} + γ²·R_{t+3} + ... = Σ γ^k · R_{t+k+1}
```

---

## 2.4 从 MDP 到 POMDP

上面的 MDP 假设 Agent 能看到环境的完整状态。但在真实 Agent 场景中，环境通常是**部分可观测**的：

- 网页 Agent 只能看到当前页面，不知道整个网站的结构
- 代码 Agent 只能看到运行结果，不知道系统内部状态
- 搜索 Agent 只能看到返回的搜索结果

**POMDP（Partially Observable Markov Decision Process，部分可观测马尔可夫决策过程）** 在 MDP 基础上增加了 Observation Space（观测空间）和 Observation Function（观测函数）。第 04 章将用完整篇幅详细展开 POMDP 的数学形式化及其在 Agentic RL 中的应用。

---

## 2.5 Exploration vs Exploitation（探索与利用）

这是 RL 的核心权衡：

- **Exploitation（利用）**：选择已知最好的动作
- **Exploration（探索）**：尝试新动作，可能发现更好的策略

在 Agentic RL 中，这体现为：
- 模型可能反复使用同一种已知的工具调用方式（Exploitation）
- 需要鼓励模型尝试新的工具组合或行动路径（Exploration）

---

## 2.6 On-Policy vs Off-Policy

| 类型 | 含义 | 代表算法 |
|------|------|---------|
| **On-Policy（同策略）** | 用当前策略产生的数据来训练 | PPO, GRPO |
| **Off-Policy（异策略）** | 可以用其他策略产生的数据来训练 | DQN, SAC |

Agentic RL 中最常用的是 **On-Policy** 算法（PPO、GRPO），因为 Agent 的行为策略需要与训练策略保持一致。

---

## 2.7 RL 的两大方法

### Policy Gradient（策略梯度）

直接优化策略 π，使其最大化期望回报：

```
∇J(θ) = E[∇log π(a|s) · G_t]
```

**直觉**：对于每个 (状态, 动作) 对，用累积回报 G_t 作为权重来调整策略参数——
- 如果 G_t 为正（结果好），增加该动作被选中的概率（梯度上升方向）
- 如果 G_t 为负（结果差），降低该动作被选中的概率（梯度下降方向）
- 用对数概率 ∇log π(a|s) 确保更新幅度与当前策略概率成反比，避免高概率动作过度更新

简而言之：**做得好的动作 → 增加概率；做得差的动作 → 降低概率**。

### Value-Based（基于价值）

学习 Value Function，然后选择价值最大的动作。

> **Actor-Critic（演员-评论家）** 结合两者：Actor（演员）是 Policy，Critic（评论家）是 Value Function。PPO 就是一种 Actor-Critic 方法。

---

## 2.8 术语速查

| 英文术语 | 中文注释 |
|---------|---------|
| Reinforcement Learning (RL) | 强化学习 |
| Markov Decision Process (MDP) | 马尔可夫决策过程 |
| Partially Observable MDP (POMDP) | 部分可观测马尔可夫决策过程 |
| State / Observation | 状态 / 观测 |
| Action | 动作 |
| Reward | 奖励 |
| Policy | 策略 |
| Value Function | 价值函数 |
| Discount Factor (γ) | 折扣因子 |
| Return / Cumulative Reward | 回报 / 累积奖励 |
| Exploration / Exploitation | 探索 / 利用 |
| On-Policy / Off-Policy | 同策略 / 异策略 |
| Actor-Critic | 演员-评论家 |
| Policy Gradient | 策略梯度 |

---

## 2.9 小结

- RL 的核心是 Agent 在环境中试错学习，通过 Reward 信号优化 Policy
- Agentic RL 使用 POMDP 框架，因为环境通常是部分可观测的
- 理解 Reward 设计、Exploration 和 Actor-Critic 是后续学习的基础
- 下一步：了解 LLM 的 Post-Training 流程 → [03 - LLM 与 Post-Training 基础](./03-LLM与Post-Training基础.md)

---

## 练习题

1. **概念辨析**：解释以下三组概念的区别：
   - State vs Observation
   - On-Policy vs Off-Policy
   - Exploration vs Exploitation

2. **公式理解**：MDP 五元组 $(S, A, P, R, γ)$ 中，折扣因子 $γ$ 的作用是什么？当 $γ=0$ 和 $γ=1$ 时分别代表什么含义？

3. **场景应用**：你正在训练一个网页导航 Agent。当 Agent 点击了"提交"按钮后，直到网页加载完成（约 5 秒后）才知道是否成功。这对应 RL 中的什么概念？这个场景是 MDP 还是 POMDP，为什么？

> **参考答案**：1. 见 2.2.3 和 2.6 节；2. γ=0 只关注即时奖励，γ=1 同等重视所有未来奖励；3. 对应延迟奖励（Delayed Reward）概念，属于 POMDP，因为 Agent 在点击后无法立即知道结果。
