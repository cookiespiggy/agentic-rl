# 11 - 训练框架选型与搭建

> **学习目标**：了解主流的 Agentic RL 训练框架，选择并搭建适合你的训练环境。

---

## 11.1 主流框架对比

| 框架 | 维护方 | 特点 | 适合场景 | GitHub Stars |
|------|--------|------|---------|-------------|
| **verl** | ByteDance Seed | 生产级、支持异步 Rollout、LangGraph Agent | 多轮 Agentic RL | 6k+ |
| **TRL** | HuggingFace | 易用、集成 HuggingFace 生态 | 单轮 RL/DPO | 12k+ |
| **OpenRLHF** | 社区 | 灵活、支持多种算法 | 研究和实验 | 5k+ |
| **RAGEN** | Northwestern | 专注多轮 Agent RL、诊断工具 | Agent 研究 | 1k+ |
| **veRL-Tool** | ByteDance | verl 的扩展，支持多领域 | 多领域 Agent | - |

---

## 11.2 推荐框架：verl

**verl（Volcano Engine Reinforcement Learning）** 是当前 Agentic RL 最成熟的生产级框架。

### 11.2.1 核心特性

```
✓ 异步 Rollout（Async Rollout）
  → 避免等待工具调用时 GPU 空闲

✓ 多轮对话和工具调用支持
  → 原生支持 Multi-turn + Tool Call

✓ Server-based 架构
  → 推理引擎（Server）和 Agent（Client）分离
  → 支持 SGLang 和 vLLM 推理引擎

✓ LangGraph Agent 集成
  → 支持自定义复杂 Agent 流程

✓ GRPO / PPO / DAPO 算法
  → 内置主流算法实现

✓ FSDP / DeepSpeed 分布式训练
  → 支持多 GPU / 多节点训练
```

### 11.2.2 系统架构

```
┌─────────────────────────────────────────────────────┐
│                    verl System                       │
│                                                     │
│  ┌──────────────┐     ┌──────────────────────┐      │
│  │  Agent Loop  │     │   Async Server       │      │
│  │  (Client)    │────→│   (推理引擎网关)      │      │
│  │              │←────│                      │      │
│  │ - 执行Agent  │     │ - SGLang Backend     │      │
│  │ - 调用工具   │     │ - vLLM Backend       │      │
│  │ - 管理对话   │     │ - Load Balancing     │      │
│  └──────────────┘     └──────────────────────┘      │
│         │                     │                      │
│         ▼                     ▼                      │
│  ┌──────────────┐     ┌──────────────────────┐      │
│  │  Environment │     │   GPU Cluster        │      │
│  │  (工具/环境)  │     │   (训练 + 推理)      │      │
│  └──────────────┘     └──────────────────────┘      │
└─────────────────────────────────────────────────────┘
```

---

## 11.3 环境搭建

### 11.3.1 硬件需求

| 模型规模 | 最低 GPU 配置 | 推荐 GPU 配置 |
|---------|-------------|-------------|
| 1B-3B | 1×A100 80G | 2×A100 80G |
| 7B-8B | 2×A100 80G | 4×A100 80G |
| 14B | 4×A100 80G | 8×A100 80G |

> 对于学习和实验，推荐使用 3B 以下的小模型 + 1-2 张 GPU。

### 11.3.2 安装 verl

```bash
# 1. 创建虚拟环境
conda create -n agentic-rl python=3.11
conda activate agentic-rl

# 2. 安装 PyTorch（根据你的 CUDA 版本）
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# 3. 安装 verl
pip install verl[vllm]  # 使用 vLLM 引擎
# 或者
pip install verl[sglang]  # 使用 SGLang 引擎

# 4. 安装推理引擎
pip install vllm==0.6.3
# 或
pip install sglang[all]

# 5. 安装额外依赖
pip install flash-attn --no-build-isolation
pip install ray  # 分布式计算
pip install wandb  # 实验追踪
```

### 11.3.3 验证安装

```python
import verl
print(f"verl version: {verl.__version__}")

import torch
print(f"PyTorch version: {torch.__version__}")
print(f"CUDA available: {torch.cuda.is_available()}")
print(f"GPU count: {torch.cuda.device_count()}")
```

---

## 11.4 备选方案：TRL

如果你只想做单轮 RL/DPO 实验，TRL（Transformer Reinforcement Learning）更简单：

```bash
pip install trl
```

```python
from datasets import load_dataset
from trl import GRPOTrainer, GRPOConfig

def my_reward_func(completions, **kwargs):
    """示例奖励函数：检查回复是否包含正确答案"""
    return [1.0 if "42" in c else 0.0 for c in completions]

dataset = load_dataset("json", data_files="my_data.json", split="train")

config = GRPOConfig(
    output_dir="./grpo_output",
    num_generations=4,
    per_device_train_batch_size=2,
    learning_rate=1e-6,
    max_steps=100,
)

trainer = GRPOTrainer(
    model="Qwen/Qwen2.5-3B",
    reward_funcs=[my_reward_func],
    args=config,
    train_dataset=dataset,
)
trainer.train()
```

---

## 11.5 其他重要工具

| 工具 | 用途 |
|------|------|
| **Ray** | 分布式计算框架，verl 依赖 |
| **Weights & Biases (wandb)** | 实验追踪和可视化 |
| **MLflow** | 替代 wandb 的实验追踪 |
| **DeepSpeed** | 分布式训练优化 |
| **FSDP** | PyTorch 原生分布式训练 |
| **FlashAttention** | 高效注意力计算 |
| **vLLM / SGLang** | 高性能 LLM 推理引擎 |

---

## 11.6 术语速查

| 英文术语 | 中文注释 |
|---------|---------|
| verl (Volcano Engine RL) | 火山引擎强化学习框架 |
| TRL (Transformer RL) | HuggingFace 的 Transformer RL 库 |
| Async Rollout | 异步展开 |
| Server-based Architecture | 基于服务的架构 |
| FSDP (Fully Sharded Data Parallel) | 全分片数据并行 |
| DeepSpeed | 微软的分布式训练框架 |
| Inference Engine | 推理引擎 |
| Load Balancing | 负载均衡 |

---

## 11.7 小结

- verl 是 Agentic RL 的首选框架，支持异步 Rollout 和多轮工具调用
- TRL 适合单轮实验和快速原型
- 根据模型大小选择合适的 GPU 配置
- 下一步：准备数据 → [12 - 数据准备与 SFT 阶段](./12-数据准备与SFT阶段.md)
